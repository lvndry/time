{
  "name": "time",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "compile": "bun build index.ts --outfile time --compile",
    "run": "bun run index.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "bun-create": {
    "start": "./time"
  }
}
